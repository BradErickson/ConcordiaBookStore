<div class="row">
    <div ng-controller="CreateMessageController">
        <div ng-show="isLoading" class="loader">loader...</div>
        <div ng-hide="isLoading">
            <form novalidate class="simple-form">
                <label for="title">Title: <input type="text" id="title" class="oneColumn" ng-model="Message.Title" /></label><br />
                <label for="authorName" title="If more then one name is on the book, type in the first name that appears">Author Name: <input type="text" id="authorName" class="oneColumn" ng-model="Message.MessageBody" /></label><br />
                <input type="submit" ng-click="update(Message)" value="Save" />
            </form>
        </div>
    </div>
</div>
