<div class="row">
    <div ng-controller="BookIndexController">
        @Html.ActionLink("Create", "Create")
        <div class="container">
            <h2>Available Books</h2>

            <table class="table table-hover">
                <thead>
                        <tr>
                            <th>Title</th>
                            <th>Author</th>
                            <th>ISBN</th>
                            <th>Genre</th>
                            <th>Sale Price</th>
                            <th>Rent Price</th>
                        </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="book in books">
                        <td>{{book.title}}</td>
                        <td ng-repeat="author in book.authors">{{author.name}}</td>
                        <td>{{book.isbn}}</td>
                        <td>{{book.genre}}</td>
                        <td>{{book.sellingPrice}}</td>
                        <td>{{book.rentingPrice}}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        {{books}}
    </div>
</div>
@*<script src="~/Client/Books/GetBookService.js"></script>
    <script src="~/Client/Books/CreateBookController.js"></script>*@
